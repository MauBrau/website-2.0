import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#D4D4D4'
        },
        secondary: {
            main: '#fff'
        }
    },
});

export default theme;